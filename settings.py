"""Shared settings for the scripts in this repo. Edit the values below."""

# Seconds the last frame is held as a still at the end of a rendered video.
# 0 disables the extra still frames. Used by the pngs_to_mp4*.py scripts.
END_HOLD_SECONDS = 0
